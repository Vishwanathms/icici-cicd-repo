pipeline {
    agent { label 'java' }
    tools { maven 'maven-3.9.8' }

    stages {
        stage('checkout') {
            steps {
                sh "ls -l"
                sh "ssh-keyscan -t rsa  github.com >> /var/jenkins_home/.ssh/known_hosts"
            }
        }
        stage('Build') {
            steps {
                echo 'Building...' 
                sh "mvn clean package -DskipTests"
                // Add build steps here
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Add test steps here
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add deploy steps here
            }
        }
    }
}